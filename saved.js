document.addEventListener('DOMContentLoaded',async function(){await MISA.ready;const grid=document.getElementById('savedGrid');const products=MISA.getProducts().filter(p=>!p.hidden&&MISA.getSavedIds().includes(p.id));grid.innerHTML=products.length?products.map(p=>MISA.renderProductCard(p,true,true)).join(''):'<p class="empty-state">You have not saved any products yet. Visit <a href="index.html">Home</a> to discover products.</p>';});
